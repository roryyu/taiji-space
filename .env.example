# PostgreSQL 连接串
DATABASE_URL="postgresql://user:password@localhost:5432/taiji_space"

# JWT 签名密钥（生产环境务必更换为高强度随机串）
JWT_SECRET="change-me-in-production"

# seed 初始管理员密码（可选，不设置则随机生成并在 seed 输出中打印一次）
# SEED_ADMIN_PASSWORD=
